import { Editor } from '../index'
import { IPhotoTrackItem } from '@clipwiz/shared'


export const addPhotoNode = (editor: Editor, trackId: string, item: IPhotoTrackItem) => {
  console.log(editor, trackId, item)
}
