# Video Player SDK Rule (`packages/videoPlayer`)

`packages/videoPlayer` 是项目的视频播放 SDK 封装，承载 VideoContext 播放与渲染能力。

凡是涉及该目录下文件的新增、修改或删除，必须在写代码前先与用户确认，说明：

- 拟修改的文件和范围；
- 修改原因与预期行为；
- 对 SDK API、播放渲染、前后端兼容性的影响。

在获得用户明确确认前，不得开始修改 `packages/videoPlayer` 下的代码，也不得通过其他目录的改动绕过该确认。
